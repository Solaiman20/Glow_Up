import 'package:flutter/material.dart';

class ProviderBookingConfirmationScreen extends StatelessWidget {
  const ProviderBookingConfirmationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
